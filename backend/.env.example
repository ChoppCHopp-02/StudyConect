# Database
DB_HOST=
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=
DB_SYNC=false

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Server
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Email (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Supabase (nếu dùng supabase-js thay Sequelize)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
